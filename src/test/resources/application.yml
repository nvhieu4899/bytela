spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: hieunv
    password: nguyenvanhieu
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
config:
  shortLinkSize: 7